sudo:            required

notifications:
  email:
    on_success:  always
    on_failure:  never

service:         docker

language:        go

go:
  - 1.8

before_script:
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure

script:
  - make gbuild

before_install:  cd testbed

install:
  - ./e2e-etcd2.sh
  - ./e2e-etcd3.sh
